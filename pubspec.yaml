name: adaptive_theme_fluent_ui
description: An extension of adaptive_theme that supports Fluent UI.
version: 0.0.1
homepage: https://github.com/birjuvachhani/adaptive_theme_fluent_ui

environment:
  sdk: ">=2.17.3 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter: